<h1 class="title is-4">Editing Page</h1>

<%= render 'form', page: @page %>

  <p class="mt-3">
  <%= link_to @page.title, permalink_path(@page.permalink) %> |
  <%= link_to 'Back', pages_path %>
</p>