<h2 class="title is-2">Top Production Company</h2>

<ol>
  <% @production_companies.each do |company| %>
    <li><%= link_to company.name,  company %>
    <em> (<%= pluralize company.movies.count, "Top Movie" %>)</em></li>
  <% end %>
</ol>
